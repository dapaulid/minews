[project]
name = "minews"
version = "0.1.0"
description = "Minimalist news briefing system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openrouter>=0.0.15",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]
